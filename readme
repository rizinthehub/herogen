2. Delete old virtual environment

In terminal:

Remove-Item -Recurse -Force .venv
3. Create new environment
py -3.10 -m venv .venv
4. Activate it
.\.venv\Scripts\Activate.ps1

You should see:

(.venv) PS D:\Local Repository\herogen>

pip install -r requirements.txt